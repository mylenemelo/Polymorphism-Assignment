# Polymorphism-Assignment

- C# console application  
- Creates an IQuittable interface with a Quit() method  
- Employee class implements the interface  
- Demonstrates polymorphism by calling Quit() through the interface  
- Simple practice of interfaces and inheritance
